#include <iostream>
#include <vector>
using namespace std;

int main() {
    int N;
    cin >> N;
    vector<int> s(N);
    for (int i = 0; i < N; ++i) {
        cin >> s[i];
    }for (int i = 0; i < N; ++i) {
        cin >> s[i];
    }
}