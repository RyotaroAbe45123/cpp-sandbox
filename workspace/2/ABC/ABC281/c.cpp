#include <iostream>
#include <vector>
using namespace std;

int main() {
    int N;
    long long T;
    cin >> N >> T;
    vector<int> A(N);
    for (int i = 0; i < N; ++i) {
        cin >> A[i];
    }
}